import useActions from './actions';
import { initialState, reducer } from './reducer';

// export the required values that will be combined in the overall StoreProvider
export { useActions, initialState, reducer };
